### Note
- `Becarefull` With host_permissions (host is the auth sever) each will automatic save the Cookie. In normal case without these permission cookie is not allow to save, it mean host can't identify our sequence of requests.